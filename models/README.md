# models/ directory
#
# Place your pre-trained EfficientNet-B2 waste classification weights here.
# Filename expected: efficientnet_b2_waste.pth
#
# If no custom weights file is found, the system automatically falls back to
# ImageNet pre-trained EfficientNet-B2 weights (demo mode).
#
# To obtain fine-tuned weights, refer to the detect-waste training scripts:
# https://github.com/wimlds-trojmiasto/detect-waste
